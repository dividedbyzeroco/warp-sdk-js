// @flow
/**
 * References
 */
export type WarpOptionsType = {
    apiKey: string,
    masterKey?: string,
    serverURL?: string,
    api?: Object,
    sessionToken?: string,
    platform?: string,
    timeout?: number,
    maxRequests?: number,
    supportLegacy?: boolean
}